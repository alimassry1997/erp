const RelatedTeamsProject = ({ name, id }) => {
  return (
    <div>
      <h3>{name}</h3> <button className="btn dark-btn"> Assign Roles</button>
    </div>
  );
};

export default RelatedTeamsProject;
